# Decision Log — VNPT-CSDL-NLD-BE

> Master, append-only decision log for this repo. Never renumbered or edited in place — a reversal adds a new row that names the row it reverses. Last reviewed: 2026-09-16.

| # | Decision | Why |
|---|---|---|
| D1 | **Clean Architecture layering**: backend chia thành 4 project — `Dashboard.API`, `Dashboard.Application`, `Dashboard.Domain`, `Dashboard.Infrastructure` — cộng `Library/` chứa các `BuildingBlocks.*` binary dùng chung (Contracts, CrossCutting, FileStorage, Infrastructure, Kafka, MinIO, StackExchangeRedis, SharedKernel). | Tách rõ API/Application/Domain/Infrastructure để dễ test, dễ thay Infrastructure (Redis/Kafka/Dell ECS) mà không đụng business logic. |
| D2 | **Kiến trúc mục tiêu (xem `src.txt`)**: ngoài slice `LabourDashboard` (đã chạy), kiến trúc đã hoạch định sẵn chỗ cho `LabourIdentity`, `LabourInsurance`, các abstraction dùng chung (`Aggregation`, `Cache`, `Dimension`, `Snapshots`) và các repository tổng quát ở tầng Domain/Infrastructure — hiện là scaffold rỗng, sẽ triển khai dần theo roadmap D01→D04. | Giữ đúng khung kiến trúc đã thiết kế để các slice sau (Identity/Insurance) đi theo cùng pattern với `LabourDashboard`, tránh mỗi feature một kiểu. |
| D3 | **Tầng Aggregate (Snapshot → Summary) triển khai theo rule-engine, không hard-code công thức**: `LabourAggregationService` (Infrastructure/Aggregates) phân loại từng bản ghi (EconomicStatusCode, AgeGroupCode) bằng `Domain.Rules.AnalyticalRuleEvaluator` đọc `DimAnalyticalRule`/`DimAnalyticalRuleCondition` từ DB, thay vì if/else cứng trong C#. Còn 4 điểm CHƯA chốt, đã đánh dấu TODO trong code: (1) chưa có bảng/entity `ProcessingRun` dù `Summary.ProcessingRunId` đã tồn tại sẵn trong schema; (2) `AdministrativeUnitCode` tạm suy ra trực tiếp từ `PermanentAddressCode`, chưa qua mapping chính thức; (3) `TopLevelOccupationCode` tạm dùng `OccupationCode` (leaf), chưa quy về phân cấp qua `DimOccupation`; (4) `WorkingAgeFrom/To` (15/60) là giá trị mặc định tham khảo theo GSO, chưa phải công thức chính thức. Cơ chế trigger (scheduled worker/Kafka/endpoint) cũng chưa chốt. | Cho phép bắt đầu code phần tính toán ngay (theo yêu cầu 16/09) dù công thức KPI chính thức và chuẩn field dữ liệu (A02/A03/A06/A19/B05/A08) vẫn đang chờ VNPT xác nhận — rule engine tách công thức ra khỏi code nên khi VNPT xác nhận, chỉ cần nạp lại DATA (dim_analytical_rule*), không cần build lại service. Không dùng số liệu do 4 điểm TODO trên sinh ra cho báo cáo chính thức trước khi xác nhận lại. |
| D4 | **Ap dung Git Flow noi bo chinh thuc** (`docs/standards/git-flow.md`, tai lieu VNPT/TFO ban hanh 16/09): tao 2 nhanh dai han `main` (SIT/UAT noi bo, nguon MR bang giao sang `develop` cua VNPT) va `develop` (tich hop hang ngay), rebase lai commit khoi tao repo theo dung format Conventional Commits + ma task (`chore(D01): ...`) thay vi commit tu do ban dau. Secret handling doi tu "redact-in-place trong file da commit" sang dung `.example` + `.gitignore` that: xoa `appsettings.Development.json` khoi git index, giu file that (chi placeholder o may nay) o local, khong track; them `appsettings.Development.json.example`. Them pre-commit hook quet pattern secret co ban (`scripts/git-hooks/pre-commit` + `scripts/setup-hooks.sh`) vi may dev hien khong co san gitleaks/git-secrets. **Chua lam duoc** (phu thuoc ha tang, khong tu lam duoc tu phia assistant): branch protection that tren GitHub cho `main`/`develop`, pipeline CI (build/test/secret-scan) chay tren PR, thay hook regex tam thoi bang gitleaks that. Repo da co remote GitHub rong (`origin`) nhung chua tung push — quy trinh PR/review/squash-merge o muc 6-7 cua tai lieu se ap dung tu cac task tiep theo (feature/fix/... branch tu `develop`), khong retroactive cho commit khoi tao nay. | Nguoi dung xac nhan (16/09) commit/nhanh ban dau **chua khop ten va noi dung** voi Git Flow chinh thuc vua duoc chia se — sua ngay tai goc truoc khi co them commit nao khac de tranh phai viet lai lich su sau nay; secret handling redact-in-place tuy da an gia tri that nhung khong dung quy uoc `.example`/`.gitignore` ma tai lieu yeu cau, nen chuyen sang dung quy uoc chinh thuc. |
| D5 | **EF Core Migration `InitialCreate` + demo seed toi thieu** (`feature/d01-05-ef-core-migration-seed`): them `Microsoft.EntityFrameworkCore.Design` vao `Dashboard.Infrastructure` + `Dashboard.API`, local tool manifest `.config/dotnet-tools.json` (`dotnet-ef` pinned 9.0.9), generate migration `InitialCreate` tu `DashboardDbContext` hien tai (15 bang: 5 dim reference + 5 dim analytical + 2 snapshot + 3 summary). Them `DashboardDemoDataSeeder` (`Dashboard.Infrastructure/Data/Seed/`) seed: `dim_administrative_unit` (5 tinh), `dim_catalog_items` (GENDER, ECONOMIC_ACTIVITY, EDUCATION_LEVEL - 7 cap), `dim_industry` (5 nganh cap 1), `dim_analytical`/`dim_analytical_item`/`dim_analytical_item_translation` cho `AGE_GROUP` (9 nhom tuoi, KHONG seed rule/condition), va **so lieu demo dien tay** (khong qua Aggregation/rule engine that) vao `labour_economic_status_summary` (24 dong, ky 2025-06/2026-06) + `labour_time_summary` (6 ky thang 2026-01..06) de 9 endpoint cua `LabourDashboardController` tra ve du lieu that. Seed chi chay khi `Database:SeedDemoData=true` (mac dinh false, chi bat trong `appsettings.Local.json` khong track git) VA environment la `Development`/`Local`; `Database.MigrateAsync()` cung chi tu-chay trong 2 environment nay. Test thu công tren DB local rieng **`vnpt_csdl_nld_be_local`** (KHONG dung chung `dashboard_local` - DB do dang mang schema cua repo `vnpt-dashboard-service`, mot repo/du an khac): ca 9 endpoint tra ve JSON hop le. **Khong bao gom**: seed `Dashboard.Domain.Rules.AnalyticalRuleEvaluator`/`DimAnalyticalRule`/`DimAnalyticalRuleCondition` cho `ECONOMIC_STATUS` (cong thuc phan loai kinh te chinh thuc van dang cho VNPT xac nhan - xem D3), seed Snapshot/`LabourIdentitySummary` (khong endpoint nao dang doc 2 bang nay). | Gap chan lon nhat truoc do (xem `Claude outputs/BACKEND_STATUS_AND_GAPS.md` muc 2.1): repo khong co migration/SQL nao nen khong endpoint nao chay duoc tren Postgres that. So lieu demo **KHONG duoc dung cho bao cao chinh thuc** - chi phuc vu dev/test API contract cuc bo, tach biet hoan toan voi cong thuc KPI/du lieu that dang cho VNPT xac nhan (D3). |
| D6 | **Chua trien khai JWT/Keycloak/`[Authorize]`** o dot D01-05/06/07: da tim trong `docs/` cua repo nay nhung KHONG thay tai lieu thiet ke claim `phan_quyen`/LLD W01 duoc nhac trong audit truoc (`Claude outputs/BACKEND_STATUS_AND_GAPS.md` muc 5.4) - tai lieu do (neu co) dang nam o mot repo/nguon khac, khong duoc coi la nguon cho repo nay. Khong tu bia cau truc claim, Keycloak realm/issuer/client id. `[Authorize]` van de comment tren `LabourDashboardController`. | Theo dung nguyen tac cua chinh decision log nay (D3: "khong tu quyet dinh thay VNPT") va yeu cau cua nguoi dung khi giao viec D01-05..07: khong lay/suy dien tu nguon cua du an/repo khac. Can Kan hoac VNPT cung cap thiet ke claim/phan quyen (hoac xac nhan lai LLD W01 nam o dau) truoc khi trien khai JWT/Keycloak that. |
| D7 | **Wire Redis cache DI** (`feature/d01-06-redis-cache-wiring`): implement `IDashboardCache` (`Dashboard.Application/Abstractions/Cache/`) + `DashboardRedisCache`/`DashboardRedisKeys` (`Dashboard.Infrastructure/Redis/`) tren `StackExchange.Redis` (them package reference vao `Dashboard.Infrastructure`, truoc chi co o `Dashboard.API`). `Program.cs` chi dang ky `IConnectionMultiplexer`/`IDashboardCache` khi co cau hinh `Redis:ConnectionString`; dung `ConfigurationOptions.AbortOnConnectFail = false` va bat loi trong tung thao tac cua `DashboardRedisCache` (log Warning, tra ve cache-miss) de app khong crash/cham khoi dong khi Redis chua san sang - da test thuc te tren may nay (khong co Redis server) va xac nhan app + 9 endpoint van chay binh thuong. **Khong bao gom**: chua co handler/repository nao trong `LabourDashboard` thuc su goi `IDashboardCache` (cache-per-endpoint, TTL, cache-key convention theo tung widget la quyet dinh rieng, ngoai pham vi dot nay) - day moi la ha tang cache san sang de dung, chua duoc ap dung vao luong doc du lieu hien co. | Gap #3 trong `Claude outputs/BACKEND_STATUS_AND_GAPS.md` (Redis package co san nhung chua DI). Uu tien "khong duoc crash/cham API vi Redis" theo nguyen tac kien truc chung (cache-aside): Redis la performance cache, khong phai source of truth cua repo nay. |
